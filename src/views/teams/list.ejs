<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <%- include('../partials/header') %>

    <main class="app-main">
        <section class="page-intro">
            <span class="page-kicker">
                <i class="bi bi-people"></i>
                Teams
            </span>
            <h1 class="page-title">Team Database</h1>
            <p class="page-subtitle">
                Filter teams by league and search by name.
            </p>
        </section>

        <section class="app-card app-card-muted">
            <form method="GET" action="/teams" class="row g-3">
                <div class="col-12 col-md-6">
                    <label for="search" class="form-label">Search Team</label>
                    <input
                        id="search"
                        name="search"
                        class="form-control"
                        type="search"
                        placeholder="Type team name..."
                        value="<%= filters?.search || '' %>"
                    >
                </div>
                <div class="col-12 col-md-4">
                    <label for="league" class="form-label">League</label>
                    <select id="league" name="league" class="form-select">
                        <option value="">All leagues</option>
                        <% (leagues || []).forEach(league => { %>
                            <option
                                value="<%= league.id %>"
                                <%= Number(filters?.leagueId) === Number(league.id) ? 'selected' : '' %>
                            >
                                <%= league.name %><%= league.country ? ` (${league.country})` : '' %>
                            </option>
                        <% }); %>
                    </select>
                </div>
                <div class="col-12 col-md-2 d-flex align-items-end">
                    <div class="button-row w-100">
                        <button type="submit" class="btn-app w-100">
                            <i class="bi bi-funnel"></i>
                            Apply
                        </button>
                        <a href="/teams" class="btn-app btn-app-secondary w-100">
                            <i class="bi bi-x-circle"></i>
                            Reset
                        </a>
                    </div>
                </div>
            </form>
        </section>

        <% if (teams && teams.length > 0) { %>
            <section class="app-card">
                <div class="section-header">
                    <div>
                        <h2 class="section-title">Teams</h2>
                        <p class="section-subtitle">
                            Showing <%= teams.length %> team<%= teams.length === 1 ? '' : 's' %>.
                        </p>
                    </div>
                </div>

                <div class="cards-grid teams-grid">
                    <% teams.forEach(team => { %>
                        <article class="team-card">
                            <% if (team.logo) { %>
                                <img src="<%= team.logo %>" alt="<%= team.name %> logo" class="team-logo">
                            <% } %>
                            <div class="team-meta">
                                <h3><%= team.name %></h3>
                                <p><%= team.country || 'Country not available' %></p>
                            </div>
                            <div class="button-row">
                                <a href="/teams/<%= team.id %>" class="btn-app btn-app-secondary">
                                    <i class="bi bi-list-check"></i>
                                    View
                                </a>
                                <a href="/analysis?team=<%= team.id %>" class="btn-app">
                                    <i class="bi bi-graph-up-arrow"></i>
                                    Analyze
                                </a>
                            </div>
                        </article>
                    <% }); %>
                </div>
            </section>
        <% } else { %>
            <section class="app-card">
                <div class="empty-state">
                    <h3>No teams found</h3>
                    <p>Try adjusting the search or league filter.</p>
                </div>
            </section>
        <% } %>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
