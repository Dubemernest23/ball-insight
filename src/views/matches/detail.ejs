<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <%- include('../partials/header') %>

    <main class="app-main">
        <% if (match) { %>
            <section class="page-intro">
                <span class="page-kicker">
                    <i class="bi bi-calendar-event"></i>
                    Match Detail
                </span>
                <h1 class="page-title"><%= match.home_team_name %> vs <%= match.away_team_name %></h1>
                <p class="page-subtitle">
                    <%= new Date(match.match_date).toLocaleDateString('en-US', {
                        weekday: 'long',
                        year: 'numeric',
                        month: 'long',
                        day: 'numeric'
                    }) %>
                    <% if (match.league_name) { %>
                        · <%= match.league_name %>
                    <% } %>
                    <% if (match.venue) { %>
                        · <%= match.venue %>
                    <% } %>
                </p>
            </section>

            <section class="app-card">
                <div class="section-header">
                    <div>
                        <h2 class="section-title">Final Score</h2>
                        <p class="section-subtitle">Full-time and halftime outcome.</p>
                    </div>
                    <span class="status-badge <%= match.status === 'FT' ? 'status-finished' : 'status-live' %>">
                        <%= match.status %>
                    </span>
                </div>

                <div class="match-teams">
                    <span class="team-name"><%= match.home_team_name %></span>
                    <span class="score-pill <%= match.home_score > match.away_score ? 'winner' : '' %>">
                        <%= match.home_score %> - <%= match.away_score %>
                    </span>
                    <span class="team-name right"><%= match.away_team_name %></span>
                </div>

                <% if (match.halftime_home_score !== null && match.halftime_away_score !== null) { %>
                    <p class="muted-text mt-2 mb-0">
                        HT: <%= match.halftime_home_score %> - <%= match.halftime_away_score %>
                    </p>
                <% } %>
            </section>

            <% if ((events && events.length > 0) || (homeEvents && homeEvents.length > 0) || (awayEvents && awayEvents.length > 0)) { %>
                <section class="app-card">
                    <div class="section-header">
                        <div>
                            <h2 class="section-title">Match Events</h2>
                            <p class="section-subtitle">Events grouped by team.</p>
                        </div>
                        <span class="pill-counter">
                            <i class="bi bi-list-stars"></i>
                            <%= events ? events.length : ((homeEvents?.length || 0) + (awayEvents?.length || 0)) %> events
                        </span>
                    </div>

                    <div class="row g-3">
                        <div class="col-12 col-lg-6">
                            <div class="app-card app-card-muted">
                                <h3 class="section-title mb-2"><%= match.home_team_name %></h3>
                                <% if (homeEvents && homeEvents.length > 0) { %>
                                    <div class="match-events">
                                        <% homeEvents.forEach(event => { %>
                                            <article class="event-row">
                                                <div class="event-time"><%= event.time_elapsed %>'</div>
                                                <div>
                                                    <p class="event-type"><%= event.player_name || 'Unknown Player' %> · <%= event.event_detail || event.event_type %></p>
                                                    <% if (event.comments) { %>
                                                        <p class="event-comment"><%= event.comments %></p>
                                                    <% } %>
                                                </div>
                                            </article>
                                        <% }); %>
                                    </div>
                                <% } else { %>
                                    <p class="muted-text mb-0">No recorded events.</p>
                                <% } %>
                            </div>
                        </div>

                        <div class="col-12 col-lg-6">
                            <div class="app-card app-card-muted">
                                <h3 class="section-title mb-2"><%= match.away_team_name %></h3>
                                <% if (awayEvents && awayEvents.length > 0) { %>
                                    <div class="match-events">
                                        <% awayEvents.forEach(event => { %>
                                            <article class="event-row">
                                                <div class="event-time"><%= event.time_elapsed %>'</div>
                                                <div>
                                                    <p class="event-type"><%= event.player_name || 'Unknown Player' %> · <%= event.event_detail || event.event_type %></p>
                                                    <% if (event.comments) { %>
                                                        <p class="event-comment"><%= event.comments %></p>
                                                    <% } %>
                                                </div>
                                            </article>
                                        <% }); %>
                                    </div>
                                <% } else { %>
                                    <p class="muted-text mb-0">No recorded events.</p>
                                <% } %>
                            </div>
                        </div>
                    </div>
                </section>
            <% } %>

            <section class="app-card">
                <div class="section-header">
                    <div>
                        <h2 class="section-title">Analyze Teams</h2>
                        <p class="section-subtitle">Open each club in the analysis module.</p>
                    </div>
                </div>
                <div class="button-row">
                    <a href="/analysis?team=<%= match.home_team_id %>" class="btn-app">
                        <i class="bi bi-graph-up-arrow"></i>
                        Analyze <%= match.home_team_name %>
                    </a>
                    <a href="/analysis?team=<%= match.away_team_id %>" class="btn-app btn-app-secondary">
                        <i class="bi bi-graph-up"></i>
                        Analyze <%= match.away_team_name %>
                    </a>
                    <a href="/matches" class="btn-app btn-app-secondary">
                        <i class="bi bi-arrow-left"></i>
                        Back to Matches
                    </a>
                </div>
            </section>
        <% } else { %>
            <section class="app-card">
                <div class="empty-state">
                    <h2>Match Not Found</h2>
                    <p>The requested match does not exist in the current dataset.</p>
                    <div class="button-row justify-content-center mt-3">
                        <a href="/matches" class="btn-app">
                            <i class="bi bi-trophy"></i>
                            Back to Matches
                        </a>
                    </div>
                </div>
            </section>
        <% } %>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
